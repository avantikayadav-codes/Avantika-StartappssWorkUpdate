#removeduplicates_usingset.py

set=set()
for i in range(1,11):
    a=int(input(f"Enter element {i}: "))
    set.add(a)
print(set) #We don't need to give logic for removing duplicates cause set already don't allow duplicates